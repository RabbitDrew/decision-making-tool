import type { IContentData } from "./interfaces"
const contentData:IContentData = {
    optionNavTitles: [
        "Add Option", 
        "Paste List", 
        "Clear List", 
        "Save List to File", 
        "Load List from File"
      ]
}

export default contentData